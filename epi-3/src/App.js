//* React Components
// Class Base (Older way of creating the components)
// Function Base (New way to create the components, Now 99% dev use function components)

// React functional components is just a JS function which return some piece of JSX

function App() {
  return (
    <div>
      <h1> from the App</h1>
    </div>
  );
}

export default App;
