import ReactDOM from "react-dom/client";
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
// This one is called component composition where we render component inside component
root.render(<App />);
